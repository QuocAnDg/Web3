import { Context as CounterContext } from '../../contexts/countersContext';
import { useContext } from 'react';
const Opinion = ({ opinion }) =>{
    const context
    return 
    <p>{opinion.vote} : {opinion.nbVote}</p>
    <Button  />
}